import { createTheme } from "@mui/material";

export const theme = createTheme({
    typography:{
        allVariants:{
            fontFamily: 'Inter, Arial, sans-serif',
            textTransform: 'none',
            fontSize: '16'
        }
    }
});